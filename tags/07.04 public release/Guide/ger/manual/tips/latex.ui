!subnode Tips & Tricks zu (!LaTeX)
!ignore_links

#################################################

!subsubnode Die eigene (!LaTeX)-Prämbel
!ignore_links

Im Gegensatz zu den vorherigen UDO-Versionen erzeugt UDO6 automatisch
passende (!LaTeX)-Präambeln für (!LaTeX)~2.09 und (!LaTeXe).

Falls Ihnen diese automatisch generierten Präambeln nicht gefallen,
können Sie UDO mit der Angabe des Schalters (!I)!/no_preamble~[tex](!i) im
Vorspann dazu bewegen, die Präambel nicht zu erzeugen.

In dem Falle können Sie mit dem Befehl (!I)!/tex(!i) oder der raw-Umgebung
zu Beginn des Quelltextes die eigene Präambel verfassen.

Das folgende Beispiel zeigt, wie das funktioniert:

!autoref [off]
!begin_quote
!vinclude examples/latex/preamble.u
!end_quote
!autoref [on]

#################################################

