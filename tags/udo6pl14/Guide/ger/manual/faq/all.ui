!subnode Allgemeine Fragen
!ignore_links
!html_name faqall

!begin_description

############################################################

!item [Was bedeutet ''FAQ''?] ~

""FAQ"" bedeutet ""Frequently Asked Questions"" und bedeutet übersetzt
soviel wie ""häufig gestellte Fragen"". Im übertragenen Sinn versteht man
darunter eine Auflistung eben dieser oft gestellten Fragen samt der
passenden Antwort.


############################################################

!item [Wieso heißt UDO ''UDO''?] ~

Als ich mit der Programmierung von UDO begann, brauchte ich einen Namen. 
Mir fiel auf die Schnelle nichts besseres ein als die Abkürzung ""UDO"", was
als Abkürzung für ""(!B)U(!b)niversal (!B)DO(!b)cument"" steht.

Übrigens: Der Autor heißt mit Vornamen nicht Udo, sondern Dirk!

############################################################

!item [Wie spricht man ''UDO'' aus] ~

''UDO'' wird genauso ausgesprochen wie der Vorname Udo, also ''Uh-
Doh''. ''UDO'' wird (!I)nicht(!i) anglophil wie ''Judo'' oder ''You
do'' ausgesprochen!


############################################################

!item [Woher bekomme ich die aktuellen Versionen?] ~

!label Bezugsquellen

Aktuelle Versionen von UDO können sich Besitzer eines Modems aus dem
Gruppenprogrammteil UDO.Pub der (!idx [Maus Iserlohn MK2]) (++49 2371 944925)
downloaden. Auf neue Versionen wird in der (!idx [MausNet])-Gruppe
(!T)ATARI.NEWS(!t) hingewiesen.

Die aktuellen Versionen sind darüber hinaus auch per (!idx [FTP]) erhältlich.
Die Archive liegen im Verzeichnis (!T)members.aol.com/UDODH/(!t)(!N)nicht
ftp.members.aol.com!(!n).

Noch einfacher ist es, in einem (!idx [WWW])-Browser die URL
(!T)http://members.aol.com/UDODH/index.htm(!t) aufzurufen, auf der man die
Links zu den jeweiligen Archiven findet.

Interessenten, die kein Modem besitzen und keinen Zugriff auf FTP-Server
oder das (!idx [World Wide Web]) haben, können die aktuellen Versionen auch durch
Einsenden einer formatierten Diskette samt adressierten und ausreichend
frankierten Rückumschlags anfordern.

!raw !tex \pagebreak[3]

Die Name der Archive der einzelnen Versionen haben jeweils einen bestimmten
Aufbau:

!begin_quote
!begin_verbatim
udorlyyy.sss
   ||| ||  |
   ||| |+--+--- .zip: Gepackt mit ST-ZIP oder PKZIP
   ||| |        .tgz: Mit gzip gepacktes tar-Archiv
   ||| |
   ||+-+------- beo:  Version für BeOS
   ||           hp4:  Version für HP-UX, 300/400er Baureihe
   ||           hp8:  Version für HP-UX, 700/800er Baureihe
   ||           lin:  Version für Linux
   ||           mac:  Version für MacOS
   ||           nex:  Version für NeXTStep
   ||           sin:  Version für Sinix
   ||           sun:  Version für SunOS
   ||           tos:  Version für Atari und Kompatible
   ||           dos:  Version für DOS, OS/2 und Windows
   ||           man:  Die Quelltexte dieser Dokumentation
   ||
   |+---------- g:    deutschsprachige Version
   |            e:    englischsprachige Version
   |
   +----------- Releasenummer: 6 für UDO Rel. 6
!end_verbatim
!end_quote

Das Archiv mit der deutschsprachigen Atari-Version des Release 6 würde also
(!T)udo6gtos.tos(!t) lauten, das mit den englischsprachigen Quelltexten
(!T)udo6eman.zip(!t).

Falls die Archive auf einem FTP-Server liegen, so können sie eventuell
aussagekräftige Namen besitzen, z.B. (!T)udo6_ger_linux.tar.gz(!t) oder
(!T)udo6_eng_hp-ux.tar.gz(!t).

############################################################

!item [Wird es Portierungen auf andere Systeme geben?] ~

Von UDO existieren momentan Versionen für folgende Systeme:

!begin_itemize !short
!item	Atari TOS
!item	DOS, OS/2, Windows
!item	HP-UX 9.x auf HP-Rechnern der 300/400er und 700/800er Baureihe
!item	Linux i86
!item	MacOS
!item	NeXTStep
!item	Sinix
!item	SunOS 5.5
!end_itemize

UDO wurde komplett in C geschrieben, verzichtet vollkommen auf
betriebssystemspezifische Routinen. Dadurch könnte die
Kommandozeilenversion von UDO theoretisch auf jedes beliebige System
portiert werden, für das ein ANSI-C-Compiler existiert.

Wenn Sie an einer Portierung auf ein bestimmtes System interessiert sind und
einen Compiler für dieses System besitzen, so schreiben Sie
(!link [mir] [Dirk Hagedorn]) einfach mal.

############################################################

!item [Können auch ''systemfremde'' Formate erzeugt werden?] ~

Mit UDO kann auf man jedem System natürlich auch Formate für andere
Betriebssysteme erzeugt werden, da die Versionen identisch sind.  So ist es
möglich, mit der Atari-Version WinHelp-Quelltexte zu erzeugen oder unter
OS/2 mit der DOS-Version ST-Guide-Quelltexte.

Lediglich eine Umsetzung der Sonderzeichen muß ggf. erfolgen. Dies kann man
z.B. mit GNU-recode durchführen. Auf Wunsch gibt es auch bei
(!link [mir] [Dirk Hagedorn])
ein Programm zur Umsetzung der Sonderzeichen in Textdateien.


############################################################

!item [Kann man Texte ""hier"" schreiben und ""dort"" konvertieren?] ~

Ja, denn UDO versteht alle Zeichensätze der System, für die UDO
erhältlich ist. Somit können Texte unter DOS geschrieben werden und
auf einem Unix-Rechner konvertiert werden.

Sogar in einer Datei können die Zeichensätze gewechselt werden, falls
das nötig sein sollte. Durch einen Satz Befehle können Sie UDO
mitteilen, welcher Zeichensatz in den folgenden Zeilen benutzt werden
soll.

############################################################

!item [Kommen in Zukunft weitere Ausgabeformate hinzu?] ~

Ich plane, die folgenden Formate in Zukunft zu unterstützen:

!begin_itemize !short
!item	Apple Guide
!item	HP-Helptag-SGML
!item	OS/2-Hilfe
!item	Portable Document Format ''PDF'', Adobe Acrobat Reader
!item	PostScript
!end_itemize

Die Unterstützung dieser Formate scheitert bisher teils an fehlender
Dokumentation, teils an der Komplexität (z.B. vom PDF), teils auch
daran, daß ich nicht weiß, ob man für ein Format Lizenzgebühren
bezahlen muß.

############################################################

!item [Kann sich die UDO-Syntax noch ändern?] ~

!index Syntaxänderungen
!index Änderungen der Syntax

UDO ist ein Programm, das ständig weiterentwickelt wird. Es werden
sicherlich auch in Zukunft weitere Kommandos hinzukommen.

Teilweise wird es sich auch nicht vermeiden lassen, die Syntax einiger
Kommandos zu ändern. Auf solche Änderungen wird aber besonders
hingewiesen, so daß man die nötigen Anpassungen innerhalb von Minuten
mit einem Editor vornehmen kann.

############################################################

!item [Wie funktioniert UDO?] ~

UDO liest die Quelldatei(en) in zwei Durchgängen ein.

Im ersten Durchgang werden Schalter, Makros, Definitionen sowie die Namen
der Kapitel für das Inhaltsverzeichnis und die automatische Referenzierung
ermittelt.

Im zweiten Durchgang erfolgt dann die Konvertierung und Formatierung des
Textes. UDO speichert bis zum Eintreffen einer Leerzeile bzw. eines
Kommandos den Text in einem internen Puffer und gibt dann den eingelesenen
Absatz aus bzw. führt das jeweilige Kommando aus.

Aus dem letzten Satz folgt: Leerzeilen (!I)und(!i) Kommandos unterteilen
Absätze.

############################################################

!item [Wie referenziert UDO?] ~

!index Referenzierung
!index Automatische Referenzierung

UDO legt bei den Hypertext-Formaten (außer beim ST-Guide-Format) im Text
automatisch (!idx [Querverweise]) zu Kapiteln und Sprungmarken an. Es werden dabei nur
Verweise auf ""andere"" Seiten angelegt, also keine Verweise auf Stellen der
aktuellen Seite.

!index !/autoref

Mit dem Schaltern (!I)!/autoref(!i) kann man die automatische
Referenzierung aus- (Parameter (!I)[off](!i)) und wieder einschalten
(Parameter (!I)[on](!i)).

############################################################

!item [Wie verweist man auf Stellen des aktuellen Kapitels?] ~

Da UDO nicht auf der Seite Verweise anlegt, auf der ein Label gesetzt wurde,
gibt es ein Problem, falls man am Seitenende einen Verweis zum Seitenanfang
anlegen will. Daher muß man hier einen manuellen Querverweis setzen.

!autoref [off]
!begin_verbatim	
!node  Test
!label Test Anfang
[...]
(!link [Zum Seitenanfang] [Test Anfang])
!end_verbatim
!autoref [on]

############################################################

!item [Wie bekomme ich Bilder in ein Inhaltsverzeichnis?] ~

Dazu muß man auf den Befehl (!I)!/tableofcontents(!i) verzichten und
ein Kapitel anlegen, in welches das Inhaltsverzeichnis ausgegeben
wird. In einem normalen Kapitel kann man natürlich Bilder wie sonst
auch benutzen. Beispiel:

!begin_quote
!begin_verbatim
!begin_document
!maketitle
!node Inhaltsverzeichnis
!image foo
!toc [all]

!node Erstes Kapitel
!end_verbatim
!end_quote

############################################################

!end_description

