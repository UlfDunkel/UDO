!subnode Fragen zum (!LaTeX)-Format
!ignore_links
!html_name faqlatex

!begin_description

#################################################
!item [Wie erzeugt UDO die (!LaTeX)-Präambel?] ~

UDO ""weiß"", in welcher Sprache und mit welchem Dokumentstil die Datei
gesetzt und ob Indizes benutzt werden sollen. Anhand dieser bekannten
Informationen bastelt UDO dann die Prämbel für (!LaTeX)~2.09 bzw. (!LaTeXe)
zusammen.

#################################################
!item [Wie werden Dateien für (!LaTeXe) erzeugt?] ~

Standardmäßig erzeugt UDO Dateien für (!LaTeX)~2.09. Durch den Schalter
(!I)!/tex_2e(!i) können Sie UDO mitteilen, Befehle für (!LaTeXe) zu
erzeugen.

#################################################
!item [Ich benötige eine eigene Präambel, was muß ich tun?] ~

Um eine eigene Prämbel für (!LaTeX) verwenden zu können, müssen Sie im
Vorspann den Schalter (!I)!/no_preamble~[tex](!i) benutzen.

#################################################
!item [Wie baue ich Formeln in den Quelltext ein?] ~

Benutzen Sie dazu die raw-Umgebung für ganze Absätze oder Definitionen
für Formeln, die im Fließtext erscheinen sollen. Beispiel:

!autoref [off]
!begin_verbatim
!ifdest [tex]
!define ab2 $(a+b)^2 = a^2 + 2ab + b^2$
!else
!macro  ab2 (a + b)^2 = a^2 + 2ab + b^2
!endif
...
Die erste binomische Formel lautet: (!ab2)
!end_verbatim
!autoref [on]

#################################################
!item [Wie werden Sonderzeichen in Indizes angepaßt?] ~

Sonderzeichen in Indizes werden speziell für (!V)makeindex(!v)
angepaßt. Es sind also keine zusätzlichen Anpassungen des von UDO
erzeugten (!LaTeX)-Files nötig.

#################################################
!item [Geschweifte Klammern im Indexeintrag erzeugt (!LaTeX)-Fehler!?] ~

Laut Kopka ist es zwingend erforderlich, daß jede öffnende geschweifte
Klammer in einem Indexeintrag eine passende schließende geschweifte
Klammer erhält. (!I)!/index~{(!i) ist demnach nicht möglich, nur
(!I)!/index~{!..}(!i) funktioniert.

#################################################

!end_description

