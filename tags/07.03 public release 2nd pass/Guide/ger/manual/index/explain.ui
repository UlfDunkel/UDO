!subnode* Befehlserklärung

!begin_description

!item	[Typ & Position:]	~ (!nl)
	Der Typ des Befehls und die Position, wo der Befehl benutzt werden darf.
	
	Ein Kommando veranlaßt UDO, etwas bestimmtes zu tun, ein Schalter setzt
    bestimmte Informationen, an denen UDO erkennt, wie es demnächst zu
    verfahren hat, ein Platzhalter wird bei der Umwandlung lediglich
    durch etwas anderes ersetzt.

	Bei ""Vorspann"" darf man einen Befehl nur im Vorspann des Quelltextes
    (also vor (!I)!/begin_document(!i)) einsetzen, bei ""Hauptteil"" darf
    man einen Befehl nur im Hauptteil des Quelltextes (also nach
    (!I)!/begin_document(!i)) einsetzen, bei ""Vorspann & Hauptteil"" darf
    man einen Befehl überall einsetzen.

!item	[Syntax:]	~ (!nl)
	Hier sieht man, wie man den Befehl einsetzen muß.

	!begin_tlist [WzeichenW] !short

	!label <text>
	!item [<text>] steht für eine Zeichenfolge,

	!label <datei>
	!item [<datei>] steht für einen Dateinamen,

	!label <wert>
	!item [<wert>] steht für eine Ziffernfolge,

	!label <zeichen>
	!item [<zeichen>] steht für ein einzelnes Zeichen,

	!label <kürzel>
	!item [<kürzel>] steht für die Angabe von Kürzeln der
	Ausgabeformate(!N)aqv = Apple QuickView,
		asc = ASCII,
		htag = HP-Helptag-SGML,
		html = HTML,
		info = Texinfo,
		ldoc = Linuxdoc-SGML,
		lyx = LyX,
		man = Manualpage,
		pch = Pure-C-Help,
		rtf = RTF,
		stg = ST-Guide,
		tex = (!LaTeX),
		tvh = Turbo-Vision-Help,
		win = WinHelp(!n)
	
	!label <sprache>
	!item [<sprache>] steht für die Angabe von
	Sprachkürzeln(!N)english = English,
		french = Französisch,
		german = Deutsch,
		italian = Italienisch,
		spanish = Spanisch,
		swedish = Schwedisch(!n)

	!label <systeme>
	!item [<systeme>] steht für ein oder mehrere
	Betriebssysteme(!N)beos = BeOS,
		dos = DOS,
		hpux = HP-UX,
		linux = Linux,
		macos = MacOS,
		nextstep = NeXTStep,
		sinix = SINIX,
		sunos = SunOS,
		tos = TOS(!n)

	!end_tlist

!item	[Beispiel:]	~ (!nl)
	Hier findet man eventuell ein kurzes Beispiel für den jeweiligen Befehl.

!item	[Siehe auch:]	~ (!nl)
	Hier sind verwandte Befehle oder Themen ausgelistet, die
	in direktem Zusammenhang mit dem beschriebenen Befehl
	stehen.

!end_description


