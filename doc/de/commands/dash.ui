!begin_node (--)
!html_name cmd_dash

Kurzen Gedankenstrich ausgeben.

!begin_xlist [(!TYPEPOSITION)]

!item [(!TYPEPOSITION)] (!SPECIALCHAR), (!PREAMBLE) & (!MAINPART)

!item [(!SYNTAX)] (!T)(--)(!t)

!item [(!DESCRIPTION)]
Zwei aufeinanderfolgende Minuszeichen werden von UDO
in einen kurzen Gedankenstrich umgewandelt, sofern
das Ausgabeformat Gedankenstriche anzeigen kann.
Andernfalls werden die beiden Minuszeichen durch
eines ersetzt.

(!NOTE) Im HTML-Format wird (!code [&ndash;]) ausgegeben. Ob der
kurze Gedankenstrich tatsächlich länger aussieht als das gewöhnliche
Minus-Zeichen, hängt von der im Browser verwendeten Schriftart ab. (Bei
der im UDO-Handbuch verwendeten Verdana scheint beides identisch zu
sein.)

!item [(!SEEALSO)]
(!link [(---)] [(---)]), (!link [((--)!)] [((--)!)]), (!link [Gedankenstriche] [Gedankenstriche])

!end_xlist

(!EXAMPLE)
(!EXAMPLE_BEFORE)
!begin_verbatim
Er träumte -- und das mitten im Winter -- !\ 
von einer Südsee-Palmeninsel.
!end_verbatim
(!EXAMPLE_AFTER)
Er träumte -- und das mitten im Winter -- von einer Südsee-Palmeninsel.
(!EXAMPLE_END)

!end_node
