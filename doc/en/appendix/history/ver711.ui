!begin_node Changes in Version 7.11
!html_name  history_0711

(!PUBLISHED [UDO~7.11][November 8, 2013])

!begin_description !compressed
!item [(!KW [toc_title])]
      new command

!item [(!KW [stg_limage])]
      new command

!item [(!KW [ignore_toptoc])]
      new command

!item [(!KW [use_auto_toptocs])]
      Does not generate links to table of contents any more
      if !tableofcontents has not been used.

!item [(!KKW [link])]
      Allow explicit links to index page for formats where
      the name of the index is known.

!item [(!KW [maketitle])]
!item [(!KW [tableofcontents])]
      For ST-Guide, allow use of !raw commands to be placed
      in the generated nodes.
      
!item [Kleinere Fehlerkorrekturen]
      Wie immer wurden verschiedene kleine Fehler korrigiert.
      Sie waren so klein, dass es nicht lohnt, sie aufzuzählen.
      
!item [(!KKKW [G] [g])]
!item [(!KKKW [S] [s])]
!item [(!KKKW [O] [o])]
      New text attributes.

!item [(!KKW [xlink])]
      Checks the optional 3rd parameter for "_new" and generates
      ALINK instead of LINK (open link in a new window). Opening
      links in a new window does not work in ST-Guide, but HypView
      honors them.

!item [(!KKW [url])]
      For ST-Guide, will be translated to an RX command.
      Allows sending those links to an application that
      has been defined with @hostname.

!end_description

!end_node
