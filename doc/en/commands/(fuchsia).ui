!begin_node (!kkw [fuchsia])
!html_name cmd_fuchsia

Sets the textcolor fuchsia.

!begin_xlist [(!TYPEPOSITION)]

!item [(!TYPEPOSITION)] (!PLACEHOLDER), (!MAINPART)

!item [(!SYNTAX)] (!T)(!/fuchsia)(!t)

!item [(!DESCRIPTION)]
Text following this placeholder will be displayed in magenta color.
You can revert to the default color using the placeholder (!kkw [coloff]).

(!COLORSUPPORT)

!item [(!EXISTSSINCE)] Version~6.5.9

!item [(!SEEALSO)]
(!KKW [coloff]), (!link [Text colors] [Text colors])

!end_xlist

(!EXAMPLE)
(!EXAMPLE_BEFORE)
!begin_verbatim
The word (!fuchsia)magenta(!coloff) appears colored in magenta.
!end_verbatim
(!EXAMPLE_AFTER)
The word (!fuchsia)magenta(!coloff) appears colored in magenta.
(!EXAMPLE_END)

!end_node
