!begin_node (!kkw [beta])
!html_name cmd_beta()

Print the (!beta) symbol.

!begin_xlist [(!TYPEPOSITION)]

!item [(!TYPEPOSITION)] (!PLACEHOLDER), (!PREAMBLE) & (!MAINPART)

!item [(!SYNTAX)] (!T)(!/beta)(!t)

!item [(!DESCRIPTION)]
This placeholder will be replaced by the beta symbol if
the destination format supports it. If it doesn't UDO will replace
this placeholder by ""(!code [beta])"".

!item [(!SEEALSO)]
(!KKW [alpha]), (!link [Special characters] [Special characters]), (!link [Predefined macros] [Predefined macros])

!end_xlist

(!EXAMPLE)
(!EXAMPLE_BEFORE)
!begin_verbatim
(!beta)
!end_verbatim
(!EXAMPLE_AFTER)
(!beta)
(!EXAMPLE_END)

!end_node
