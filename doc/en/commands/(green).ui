!begin_node (!kkw [green])
!html_name cmd_green

Sets the textcolor green.

!begin_xlist [(!TYPEPOSITION)]

!item [(!TYPEPOSITION)] (!PLACEHOLDER), (!MAINPART)

!item [(!SYNTAX)] (!T)(!/green)(!t)

!item [(!DESCRIPTION)]
Text following this placeholder will be displayed in green color.
You can revert to the default color using the placeholder (!kkw [coloff]).

(!COLORSUPPORT)

!item [(!EXISTSSINCE)] Version~6.5.9

!item [(!SEEALSO)]
(!KKW [coloff]), (!link [Text colors] [Text colors])

!end_xlist

(!EXAMPLE)
(!EXAMPLE_BEFORE)
!begin_verbatim
The word (!green)green(!coloff) appears colored in green.
!end_verbatim
(!EXAMPLE_AFTER)
The word (!green)green(!coloff) appears colored in green.
(!EXAMPLE_END)

!end_node
