!begin_node (!kkw [gray])
!html_name cmd_gray

Sets the textcolor (dark) gray.

!begin_xlist [(!TYPEPOSITION)]

!item [(!TYPEPOSITION)] (!PLACEHOLDER), (!MAINPART)

!item [(!SYNTAX)] (!T)(!/gray)(!t)

!item [(!DESCRIPTION)]
Text following this placeholder will be displayed in gray color.
You can revert to the default color using the placeholder (!kkw [coloff]).

(!COLORSUPPORT)

!item [(!EXISTSSINCE)] Version~6.5.9

!item [(!SEEALSO)]
(!KKW [coloff]), (!link [Text colors] [Text colors])

!end_xlist

(!EXAMPLE)
(!EXAMPLE_BEFORE)
!begin_verbatim
The word (!gray)gray(!coloff) appears colored in gray.
!end_verbatim
(!EXAMPLE_AFTER)
The word (!gray)gray(!coloff) appears colored in gray.
(!EXAMPLE_END)

!end_node
