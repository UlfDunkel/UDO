!begin_node (!kkw [aqua])
!html_name cmd_aqua

Sets the textcolor aqua.

!begin_xlist [(!TYPEPOSITION)]

!item [(!TYPEPOSITION)] (!PLACEHOLDER), (!MAINPART)

!item [(!SYNTAX)] (!T)(!/aqua)(!t)

!item [(!DESCRIPTION)]
Text following this placeholder will be displayed in cyan color.
You can revert to the default color using the placeholder (!kkw [coloff]).

(!COLORSUPPORT)

!item [(!EXISTSSINCE)] Version~6.5.9

!item [(!SEEALSO)]
(!KKW [coloff]), (!link [Text colors] [Text colors])

!end_xlist

(!EXAMPLE)
(!EXAMPLE_BEFORE)
!begin_verbatim
The word (!aqua)cyan(!coloff) appears colored in cyan.
!end_verbatim
(!EXAMPLE_AFTER)
The word (!aqua)cyan(!coloff) appears colored in cyan.
(!EXAMPLE_END)

!end_node
