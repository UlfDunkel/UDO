!subsubnode Zentrieren von Zeilen
!alias center-Umgebung

Zeilen, die innerhalb einer center-Umgebung stehen, werden in der
Ausgabedatei zentriert dargestellt, sofern das jeweilige
Ausgabeformat zentrierten Text unterstützt.

Die center-Umgebung kann innerhalb anderer Umgebungen verwendet
werden, auch innerhalb einer anderen center-Umgebung(!N)so sinnlos das
auch sein mag(!n).

Werden andere Umgebungen innerhalb der center-Umgebung verwendet, dann
werden diese wie sonst auch ausgegeben. Erst wenn die center-Umgebung
wieder aktiv ist, wird Text zentriert ausgegeben.

Falls das folgende kleine Beispiel nicht zentriert wird, liegt das daran,
daß das Format der Dokumentation, die Sie gerade lesen, keine Zentrierungen zuläßt.
Aus!..

!begin_quote
!begin_verbatim
!begin_center
Eine zentrierte Zeile.

Ein zentrierter Absatz,
der vorher formatiert wird.

Die Anleitung zu (!nl)
UDO
!end_center
!end_verbatim
!end_quote

!..wird:

!begin_center
Eine zentrierte Zeile.

Ein zentrierter Absatz,
der vorher formatiert wird.

Die Anleitung zu (!nl)
UDO
!end_center

Wie man sieht, formatiert UDO Absätze auch hier. Um einen manuellen
Zeilenumbruch zu setzen, bedient man sich des Befehls (!I)(!/nl)(!i).

