!subsubnode	Fußnoten

Text, der durch (!I)(!/N)(!i) und (!I)(!/n)(!i) eingefaßt wird, wird in eine
Fußnote umgewandelt, sofern das Ausgabeformat Fußnoten unterstützt.  Bei den
anderen Ausgabeformaten wird (!I)(!/N)(!i) einfach durch ""~("" und
(!I)(!/n)(!i) durch "")"" ersetzt; die Fußnote erscheint hier also einfach
in Klammern.

Wichtig: Vor (!I)(!/N)(!i) sollte kein Leerzeichen stehen, da sonst die
Fußnotenmarkierung später in der Ausgabedatei frei im Raum steht
und der Bezug weniger klar erkenntlich wird! Unterstützt das Ausgabeformat
keine Fußnoten, gibt UDO vor der öffnen Klammer zusätzlich noch das fehlende
Leerzeichen aus.

Aus!..

!begin_quote
!begin_verbatim
UDO(!N)Universal Document(!n)
!end_verbatim
!end_quote

wird!..

!begin_quote
UDO(!N)Universal Document(!n)
!end_quote

Fußnoten werden von folgenden Formaten unterstützt:

!begin_itemize !short
!item	(!LaTeX)
!item	Linuxdoc-SGML
!item	LyX
!item	Texinfo
!item	RTF
!end_itemize


(!HINTS)

!begin_enumerate

!item Daß UDO bei den Formaten, die keine Fußnoten unterstützen, den
Fußnotentext einfach in Klammern setzt, ist natürlich etwas unglücklich.
Besser wäre es, UDO würde hier die Fußnotentexte sammeln und am Ende eines
Kapitels ausgeben. Leider ist die eine recht kniffelige Angelegenheit, die
nicht von heute auf morgen gelöst werden kann.

!item Denken Sie daran, vor (!I)(!/N)(!i) kein Leerzeichen zu verwenden.

!end_enumerate
